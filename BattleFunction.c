#include "TwistedFables.h"

int discard_back_to_deck(player *P){
	for(int i = 0 ; i < P->discard.size ; i++){
		pushbackVector(vector* vec, int32_t val)
			
		
	}
}

int8_t draw_card(int8_t amount , player *P){
	for(int i = 0 ; i < amount ; i++){
		if(P->deck.SIZE !=0){
			Card_Define(P->deck.array[P->deck.SIZE-1] , P->hands_card[hands]);
			P->deck.array[P->deck.SIZE-1] = 0; //remove card
			P->deck.SIZE--;
			P->hands++;
		}else{
			
		}
	}
	return 0;
}


